[
    {
        "id": "setup-openclaw",
        "title": "Setting up OpenClaw: The Ultimate Personal Agent",
        "summary": "A comprehensive guide to installing, configuring, and deploying your own autonomous OpenClaw agent locally or on a VPS.",
        "level": "Intermediate",
        "duration": "20 min",
        "content": "<p>OpenClaw is more than just a chatbot—it's an operating system for agentic AI. By the end of this tutorial, you'll have a fully functional agent running on your machine.</p><h3>Prerequisites</h3><ul><li>Node.js v20+</li><li>A terminal (PowerShell, Bash, or Zsh)</li><li>API Keys (OpenAI, Anthropic, or Google)</li></ul><h3>Step 1: Installation</h3><p>First, we need to install the OpenClaw core globally on your machine. Open your terminal and run:</p><div class='code-block'><pre><code>npm install -g openclaw</code></pre></div><p>Verify the installation by checking the version:</p><div class='code-block'><pre><code>openclaw --version</code></pre></div><h3>Step 2: Initialization</h3><p>Create a new workspace for your agent. This is where its memory and configuration will live.</p><div class='code-block'><pre><code>mkdir my-agent\ncd my-agent\nopenclaw init</code></pre></div><p>You'll be prompted to select a template. Choose 'Personal Assistant' for a general-purpose agent.</p><h3>Step 3: Configuration</h3><p>Open the generated <code>config.toml</code> file. You need to add your LLM provider keys here.</p><div class='code-block'><pre><code>[llm]\nprovider = \"anthropic\"\nmodel = \"claude-3-5-sonnet-20240620\"\napi_key = \"sk-ant-...\"</code></pre></div><h3>Step 4: Running the Agent</h3><p>Launch your agent in interactive mode:</p><div class='code-block'><pre><code>openclaw run</code></pre></div><p>You should now see the OpenClaw banner and a prompt waiting for your command!</p><h3>Pro Tip: Connecting to Telegram</h3><p>To make your agent accessible from your phone, run the gateway service:</p><div class='code-block'><pre><code>openclaw gateway connect --channel telegram --token YOUR_BOT_TOKEN</code></pre></div>"
    },
    {
        "id": "gemini-api",
        "title": "Integrating Gemini 1.5 Pro API",
        "summary": "Learn how to harness Google's massive 2M context window for document analysis and multimodal tasks.",
        "level": "Beginner",
        "duration": "15 min",
        "content": "<p>Google's Gemini 1.5 Pro offers an unprecedented 2 million token context window. This tutorial shows you how to build a simple script to analyze large PDF documents.</p><h3>Step 1: Get an API Key</h3><p>Head over to <a href='https://aistudio.google.com/' class='text-indigo-400 hover:text-white'>Google AI Studio</a> and click 'Get API Key'.</p><h3>Step 2: Project Setup</h3><p>Initialize a new Node.js project and install the SDK:</p><div class='code-block'><pre><code>mkdir gemini-analyzer\ncd gemini-analyzer\nnpm init -y\nnpm install @google/generative-ai</code></pre></div><h3>Step 3: The Code</h3><p>Create a file named <code>analyze.js</code> and paste the following code:</p><div class='code-block'><pre><code>const { GoogleGenerativeAI } = require(\"@google/generative-ai\");\nconst fs = require(\"fs\");\n\n// Access your API key as an environment variable\nconst genAI = new GoogleGenerativeAI(process.env.API_KEY);\n\nasync function run() {\n  // For text-only input, use the gemini-pro model\n  const model = genAI.getGenerativeModel({ model: \"gemini-1.5-pro\" });\n\n  const prompt = \"Summarize this document and find key action items.\";\n  \n  // ... (Load file logic here)\n\n  const result = await model.generateContent([prompt, ...fileParts]);\n  const response = await result.response;\n  const text = response.text();\n  console.log(text);\n}\n\nrun();</code></pre></div><h3>Step 4: Run It</h3><p>Execute your script:</p><div class='code-block'><pre><code>export API_KEY=\"your_key_here\"\nnode analyze.js</code></pre></div>"
    }
]